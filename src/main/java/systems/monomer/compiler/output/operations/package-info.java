/**
 * Contains the classes that represent the operations that can be performed by the compiler.
 * These can be either inline (e.g. add) or standalone operations (e.g. mov).
 */
package systems.monomer.compiler.output.operations;