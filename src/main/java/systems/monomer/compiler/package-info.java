/**
 * Contains helper classes for the compiler.
 * <p>
 *     {@link systems.monomer.compiler.CompileMemory} contains the memory position (relative, absolute, offset, etc) of variables for the compiler.
 *     {@link systems.monomer.compiler.CompileValue} contains the access location (ie memory, register) of values for the compiler.
 *     {@link systems.monomer.compiler.CompileSize} contains the size of values (possibly based on calculations) for the compiler.
 * </p>
 *
 * @since 1.0
 * @author jzhyang
 * @version 1.0
 */
package systems.monomer.compiler;