package systems.monomer;

public class Config {
    /**
     * MAJOR - Stores the version's major number (ie large, non-backwards compatible change)
     * MINOR - Stores the version's minor number (ie new, backward compatible feature addition)
     * REVISION - Stores the version's revision number (ie bug fixes)
     */
    public static final int MAJOR = 1, MINOR = 0, REVISION = 0;
}
