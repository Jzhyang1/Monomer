/**
 * Contains the classes that represent the values that the interpreter can work with.
 * Each class extends a corresponding class in the types package.
 */

package systems.monomer.interpreter.values;